{
  "name": "next-headless-getting-started",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "node -e 'console.log(\"NODE_ENV=\",process.env.NODE_ENV)' && next start",
    "clean": "rimraf .next node_modules",
    "lint": "next lint",
    "generate": "gqty generate",
    "wpe-build": "node -e 'console.log(\"NODE_ENV=\",process.env.NODE_ENV)' && next build"
  },
  "dependencies": {
    "@faustjs/core": "^0.14.1",
    "@faustjs/next": "^0.14.0",
    "next": "^11.1.2",
    "normalize.css": "^8.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "sass": "^1.43.4"
  },
  "devDependencies": {
    "@gqty/cli": "^2.2.0",
    "@types/react": "^17.0.33",
    "dotenv": "^10.0.0",
    "eslint": "^7.32.0",
    "eslint-config-next": "^11.1.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  }
}
